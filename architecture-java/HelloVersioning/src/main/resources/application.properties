# Configuration Spring Boot
server.port=8080

# Nom de l'application (visible dans les logs et /actuator/info)
spring.application.name=hello-versioning

# Version affichée par l'API (injectée depuis pom.xml via @Value)
app.version=1.1.0
